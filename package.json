{
  "name": "weather-pwa",
  "version": "1.0.0",
  "private": true,
  "description": "Погода — Samsung One UI style PWA with a running-conditions section",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
